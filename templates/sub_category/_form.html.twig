{{ form_start(form) }}
    <label for="name">nom de la sous categorie</label>
    {{ form_widget(form.name,{attr:{'class':'form form-control'}}) }}
    <label for="name"> categorie</label>
    {{ form_widget(form.category,{attr:{'class':'form form-control'}}) }}
    <button class="btn btn-outline-primary">{{ button_label|default('Save') }}</button><br><br>
{{ form_end(form) }}
